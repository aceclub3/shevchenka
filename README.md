# Проєкт Шевченка — сайт-галерея

**Живий сайт:** https://aceclub3.github.io/shevchenka/
**Хостинг:** GitHub Pages, безкоштовно. Локальна копія: `E:\Denys\shevchenka-site`

Дворівнева квартира 160,5 м² (106,4 + 54,1): три спальні, три санвузли,
відкрита кухня-їдальня-вітальня з другим світлом.

## Планування

Дворівнева квартира. **1-й поверх:** кухня-їдальня-вітальня, майстер-спальня з окремою
ванною кімнатою, гардеробна (постирочна — її частина, не окреме приміщення), гостьовий
санвузол, передпокій, сходи. **2-й поверх — мансарда:** дитячі спальні та спільний
санвузол, хол.

## Структура сайту

Три вкладки над однією спільною бібліотекою зображень:

- **Кімнати** — 69 візуалізацій по приміщеннях, з фільтром. На кожному фото стоїть
  тимчасовий номер (`1-14`, `2-03`) — поверх і номер кадру, щоб можна було вказати,
  що куди перекласти. Прибрати номери — видалити виклик `badge(noOf(k))` у `tile()`.
- **Меблі** — кожна ПОЗИЦІЯ окремою карткою. На одному аркуші креслень часто кілька
  різних предметів, тож вони розділені: у кожного своя назва, розміри, матеріал і
  кнопка «Посилання на позицію» (`#f=<id>`), яка відкриває сайт саме на ній —
  щоб відправити підряднику тільки його частину. Фільтр за типом: корпусні, мʼякі,
  світло, готові меблі. У картці показано **обрізаний фрагмент** аркуша саме з цим
  предметом, а якщо натиснути — аркуш повністю з **рамкою** навколо позиції.

### Звідки беруться рамки позицій

Координати рамок (`box` у `catalog.js`) визначені автоматично: креслення це темна
графіка на білому, тож `build/boxes.mjs` знаходить окремі згустки пікселів. Далі
зіставлення «позиція → рамка» зробили агенти, які дивились на аркуші з промальованими
пронумерованими рамками і звіряли розміри біля розмірних ліній.

Два нюанси, які варто знати при повторному прогоні:
- Детектор іноді ловить червону примітку «Всі розміри потребують уточнення на місці»
  вгорі аркуша як окрему рамку — це не предмет, такі рамки просто не використовуються.
- 12 рамок містять кілька позицій одразу: на аркуші намальовано складений вузол
  (наприклад, тумба + дзеркало + полиці однією композицією). Тоді підсвічується вузол,
  а картка чесно пише «тут N позиції».
- **Плани** — лише робочі плани, згруповані за типом (обмірні, перепланування,
  меблювання, сантехніка, освітлення, стелі, підлога) з фільтром. Ескізів меблів тут
  свідомо НЕМАЄ — вони живуть виключно у вкладці «Меблі», щоб не дублювати.

**Одне фото живе в одному екземплярі.** Кімната і позиція меблів лише посилаються
на нього за ключем на кшталт `f1/020`. Тому одна візуалізація може бути одночасно
в «Кухні» й у картці кухонного острова, не займаючи місця двічі й не розсинхронізовуючись.

## Файли

| Файл / папка | Що це |
|---|---|
| `index.html` | Уся сторінка — HTML, CSS і JS в одному файлі |
| `data.js` | Розміри й перелік зображень — генерується `process.mjs` |
| `catalog.js` | **Смисловий шар: кімнати, меблі, звʼязки. Правиться руками** |
| `img/f1`, `img/f2`, `img/dwg` | `full/` для перегляду + `w300 w450 w600 w900` для сітки |
| `files/kreslennya.pdf` | Оригінальний PDF креслень (66 МБ) |
| `process.mjs` | Збірка: стискає фото і рендерить сторінки PDF |
| `.nojekyll` | Каже GitHub не обробляти сайт Jekyll-ом. Не видаляти |

Оригінали дизайнера лежать окремо і не змінюються: `E:\Denys\Проект Шевченка`.

## Про якість зображень

Мініатюри ріжуться **по ширині**, а не по довгій стороні — сітка розкладає плитки
саме за шириною. Раніше портретне фото обмежувалось 700 px по висоті й давало лише
462 px ширини, через що на екрані з DPR 3 розтягувалось у 2,35 раза й виглядало
розмитим. Тепер кожне фото має чотири ширини, і браузер через `srcset` бере рівно ту,
що потрібна цьому екрану.

Виміряно на живому сайті: перший екран — **461 КБ**, прокрутка всіх 69 фото —
**2,8 МБ** на телефоні з DPR 3 і **1,0 МБ** на звичайному ноутбуці. Повний файл
(до 3000 px) підвантажується лише коли фото відкривають.

Фото для шапки лежить окремим файлом `img/hero.webp` (1800 px, 192 КБ): вона на всю
ширину екрана, тож найбільший розмір із сітки для неї замалий, а повний файл на 3000 px
займав 565 КБ — дві третини першого екрана.

Якщо міняєш кількість колонок у сітці (`.grid{columns:N}`) — **обовʼязково онови
`GRID_SIZES` у скрипті сторінки**, інакше браузер обиратиме неправильний розмір.

## Перегляд зображень

Масштабування **самої сторінки** вимкнено (`user-scalable=no` + `touch-action:pan-x pan-y`
на `html` + гасіння `gesturestart` для Safari на iPhone, який ігнорує перше й друге).
Причина: випадковий жест по мініатюрі збільшував увесь сайт і повернути 100% було майже
неможливо. Гортання сторінки при цьому працює як завжди.

Масштабуються тільки самі зображення — власним зумом усередині перегляду
(`.lb{touch-action:none}`, жести рахує скрипт).

| Дія | Телефон | Компʼютер |
|---|---|---|
| Наблизити | два пальці або подвійний тап | колесо миші або подвійний клік |
| Рухати збільшене | палець по зображенню | перетягнути мишею |
| Гортати | свайп (лише при 100%) | стрілки ‹ › або клавіші ←/→ |
| Повернути 100% | кнопка з відсотками у шапці | та сама кнопка |
| Закрити | тап по фону, ✕ або «назад» | ✕ або Esc |

## Як оновити

**Нові або замінені фото:** покласти в оригінальну папку, потім

```powershell
cd E:\Denys\shevchenka-site
npm install          # тільки перший раз
node process.mjs
git add -A; git commit -m "оновлення фото"; git push
```

**Виправити кімнату фото або звʼязок меблів із візуалізаціями:** відредагувати
`catalog.js` руками й запушити. Перезбірка зображень для цього не потрібна.

Прив'язки «креслення меблів → візуалізації» складені автоматично за кімнатою
й типом предмета, тож можуть містити неточності — це саме те, що варто вичитати.

## Що варто знати

- Ліміти GitHub Pages: 1 ГБ на сайт, 100 ГБ трафіку на місяць. Зараз сайт важить ~120 МБ.
- Сайт публічний: хто має посилання, той бачить. Пошуковики його самі не знайдуть,
  але це не приватність.
- Прибрати сайт: вимкнути Pages у налаштуваннях репозиторію або `gh repo delete aceclub3/shevchenka`.
